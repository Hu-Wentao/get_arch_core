name: get_arch_core
description: flutter architecture package, Inspired by Explicit Architecture, Clean Architectures, DDD,
version: 1.0.0
homepage: https://github.com/Hu-Wentao/get_arch_core

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  get_it: ">=4.0.0 <4.1.0"
  meta: ">=1.1.0 <1.2.0"
  rxdart: ^0.24.1
  dartz: ^0.9.1
  equatable: ">=1.2.0 <1.3.0"

dev_dependencies:
  test: ^1.15.3
  build_runner: ^1.10.0

flutter:
