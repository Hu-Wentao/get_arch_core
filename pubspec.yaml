name: get_arch_core
description: flutter architecture package, Inspired by Explicit Architecture, Clean Architectures, DDD,
version: 0.2.2
repository: https://github.com/Hu-Wentao/get_arch_core

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  get_it: ^4.0.2
  injectable: ^0.4.0+1
  rxdart: ^0.24.1
  dartz: ^0.9.1
  freezed_annotation: ^0.11.0
  equatable: ^1.2.0
  verify: ^1.1.1
  kt_dart: ^0.7.0+1

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^1.10.0
  injectable_generator: ^0.4.1

flutter:
