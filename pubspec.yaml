name: get_arch_core
description: flutter architecture package, Inspired by Explicit Architecture, Clean Architectures, DDD,
version: 2.0.0
homepage: https://github.com/Hu-Wentao/get_arch_core

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  get_it: ^5.0.4
  meta: ^1.2.4
  rxdart: ^0.25.0
  dartz: ^0.9.2
  equatable: ^1.2.5
  uuid: ^2.2.2
dev_dependencies:
  test: null
#  build_runner: ^1.10.0
#  analyzer: '<=0.39.11' # https://github.com/dart-lang/build/issues/2745 # 关于build_runner的报错

flutter:
